NEXT_PUBLIC_SUPABASE_URL=
# `sb_publishable_…` from Supabase dashboard → Settings → API keys
# (replaces the legacy anon JWT key).
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
